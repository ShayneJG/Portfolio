---
import Layout from "../layouts/Layout.astro";
import { withBase } from "../lib/url";
---

<Layout title="Shayne Geilman" pageType="home">
  <section class="hero content">
    <h1 class="hero-lead">
      Production data and LLM systems. Research editing and philosophy
      in a former life.
    </h1>
  </section>

  <hr class="rule" />

  <section class="content">
    <span class="label">Featured</span>
    <p class="placeholder">
      The featured case study will live here once the first case study is
      written.
    </p>
  </section>

  <hr class="rule" />

  <section class="content">
    <span class="label">More work</span>
    <p class="placeholder">
      The project list will live here once the case studies and personal
      projects are added to the <code>work</code> content collection.
    </p>
  </section>

  <hr class="rule" />

  <section class="about-writing">
    <div class="content">
      <span class="label">About</span>
      <p class="placeholder">
        A short teaser of the polymath narrative will live here.{" "}
        <a href={withBase("/about")}>More on /about →</a>
      </p>
    </div>
    <div class="content">
      <span class="label">Recent writing</span>
      <p class="placeholder">
        The two most recent posts will live here.{" "}
        <a href={withBase("/writing")}>All writing →</a>
      </p>
    </div>
  </section>
</Layout>

<style>
  .hero {
    padding: var(--space-3xl) 0 var(--space-2xl);
  }

  /* The home page hero is the positioning sentence as H1. The
     wordmark in the header answers "who"; the hero answers "what",
     compactly enough to hand the page off to FEATURED below. */
  .hero-lead {
    font-family: var(--font-serif);
    font-weight: 500;
    font-size: 22px;
    line-height: 1.4;
    color: var(--ink);
    max-width: var(--content-width);
  }
  @media (min-width: 768px) {
    .hero-lead {
      font-size: 26px;
      line-height: 1.35;
    }
  }

  .placeholder {
    color: var(--ink-muted);
    font-style: italic;
    font-size: 16px;
    max-width: var(--content-width);
  }
  .about-writing {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xl);
    padding: var(--space-xl) 0;
  }
  @media (min-width: 768px) {
    .about-writing {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-3xl);
    }
  }
</style>
